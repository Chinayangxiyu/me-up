

什么是双亲委派机制：
除了Bootstrap根加载器，其他的类加载都有父加载器的；在类加载的过程中；先将当前类交给父类加载器加载；
如果父类加载器无法加载；再由子加载器加载。
打破双亲委派机制
1、双亲委派是1.2提出的，为了兼容以前的代码；通过findClass()方法去加载；
2、SPI破坏了双亲委派，SPI是通过ServiceLoader，获取当前线程上下文加载器，去加载有第三方实现的服务；通常这个上下文加载器是
ApplicationClassLoader。正常的双亲委派机制的加载"路径"是向父类加载器传递；而SPI是直接指定的线程上下文去加载指定类。