# 一、常见的限流算法
（1）计数器（固定窗口）；  
（2）滑动窗口；  
（3）漏桶算法；  
（4）令牌桶算法；

在单机节点的情况下可以使用semaphore实现限流。

## 1.1 计数器实现原理

## 1.2 滑动窗口
（1）将1秒拆成n份，每份需要记录这段时间内请求的数量；
（2）使用节点数量为n的环链List（类似时间轮），每个节点保存在当前节点的时间范围内的请求数量；
（3）List的遍历，使用带有超时时间的等待，